For the Interaction Script place it on the object that you have to walk to set the 
Player,Text,range and message for it and done
there are no function that are bound to the script and the current interaction button is e
add whatever you want to do in the "DOTHIS" function

For the quiz script pretty clear multiple choice awnsers you can add however many you want to
as long as enough awnsers are supplied there is no current way to use 3 awnsers instead of 4 so make sure those are filled in